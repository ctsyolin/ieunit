This directory is intended to store extra scripts which
are useful for general public but has not been included
into the release package. 

In order to use these scripts users have to download them
from IeUnit CVS server and copy them to appropriate directories.
